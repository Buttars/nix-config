# Refactoring

- Scope strictly to the desired change — no drive-by cleanup
- Never mix refactoring with feature development — they are separate commits with separate intent
- A refactor should always aim to preserve existing behavior, not change it
- If a refactor would help, flag it and let the user decide — don't apply it unsolicited
- No new abstractions or file splits without a clear reason
